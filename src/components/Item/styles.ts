import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginTop: 16,
    borderBottomWidth: 1,
    borderBottomColor: '#E4E6EC',
    marginHorizontal: 24,
    paddingBottom: 16
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 600
  }
})